#pragma once

#include <iostream>
using namespace std;

/* A way to easily open a file dialogue
 * box using the default windows features*/

extern bool openFile();

extern string sSelectedFile;
extern string sFilePath;